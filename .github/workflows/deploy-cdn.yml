name: Deploy to CSS CDN
on:
  workflow_run:
    workflows: ['Publish to NPM']
    types:
      - completed
  workflow_dispatch:

jobs:
  build:
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'

      # @squidit/css está no npm público; evita 404 no GitHub Packages
      - name: Use npm registry for install
        run: sed -i '/@squidit:registry/d' .npmrc || true

      - name: Auth GCloud
        uses: 'google-github-actions/auth@v2'
        with:
          credentials_json: '${{ secrets.GCE_AUTH_JSON }}'

      - name: Install dependencies and build 🔧
        run: npm install && npm run build:prod

      - name: Upload CSS
        uses: 'google-github-actions/upload-cloud-storage@v2'
        with:
          path: 'dist/@squidit/ngx-css'
          destination: 'squid-css'
